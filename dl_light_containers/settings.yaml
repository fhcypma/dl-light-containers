default:
  aws:
    region: eu-west-1
    accounts:
      deploy: "092113928458"
      data: "534282717446"
      etl: "319406913669"

    tags:
      Application: "Containers"
      Environment: "@format {this.current_env}"

  # Tag for the docker image
  # Is overwritten by pipeline in DYNACONF_IMAGE_TAG
  image_tag: "local"
